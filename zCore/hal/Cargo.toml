[package]
name = "hal"
version = "0.1.0"
edition = "2021"
description = "HAL abstraction layer -- trait definitions and common types"

[features]
default = []

[dependencies]
bitflags = "1.3"
log = "0.4"
